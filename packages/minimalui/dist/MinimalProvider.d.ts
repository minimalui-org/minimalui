import * as React from "react";
import { Theme } from "./createTheme";
export declare type MinimalProviderProps = {
    theme?: Theme;
};
export declare const MinimalProvider: React.FunctionComponent<MinimalProviderProps>;
export default MinimalProvider;
